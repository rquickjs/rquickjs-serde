[package]
name = "rquickjs-serde"
description = "Serde support for rquickjs"
version = "0.6.1"
edition = "2024"
rust-version = "1.87"
license = "Apache-2.0"
repository = "https://github.com/rquickjs/rquickjs-serde"
readme = "README.md"
authors = ["Emile Fugulin <code@efugulin.com>", "The Javy Project Developers"]

[dependencies]
rquickjs = { version = "0.13", default-features = false }
serde = { version = "1", default-features = false, features = ["alloc"] }

[dev-dependencies]
quickcheck = "1"
serde = { version = "1", features = ["derive"] }
